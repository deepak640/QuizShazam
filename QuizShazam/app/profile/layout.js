export const metadata = {
  title: "My Profile",
  description: "View your quiz history, scores, and progress on QuizShazam.",
  robots: { index: false, follow: false },
};

export default function ProfileLayout({ children }) {
  return children;
}
